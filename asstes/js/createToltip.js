function hacertooltip() {

  $(".fa-lock-open").tooltip({
      title: "Libre acceso",
      animation: true
  });
  $(".fa-lock").tooltip({
      title: "Suscripción",
      animation: true
  });

}